import { RedirectPokemonDirective } from './redirect-pokemon.directive';

describe('RedirectPokemonDirective', () => {
  it('should create an instance', () => {
    const directive = new RedirectPokemonDirective();
    expect(directive).toBeTruthy();
  });
});
